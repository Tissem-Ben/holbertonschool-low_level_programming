#include <stdio.h>

/**
 * main - print the new name
 * @argc: pour argument count
 * @argv: pour argument values
 *
 * Return: Always 0 (Success).
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
			return (0);
}
